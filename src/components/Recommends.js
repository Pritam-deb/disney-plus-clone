import styled from 'styled-components';

const Recommends = (props) => {
    return(
        <div>
            <h3>
                Recommends
            </h3>
        </div>
    )
};

export default Recommends;